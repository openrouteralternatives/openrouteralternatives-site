# Brand assets

Empty by design.

`GatewayLogo` renders a stable monogram whenever a gateway record has `logo: null`, so no
entry on the site depends on hotlinking a third party's image.

To use a real mark, add a locally stored, legally usable file here and set the record's
`logo` field to its public path, for example `logo: "/logos/eden-ai.svg"`.
